import { expect } from 'chai';
import 'mocha';
describe('TesteExample tests', () => {

  it('is Hello', async () => {

    const helloHord = "Hello"

    expect(helloHord).to.equal("Hello")

  });

});